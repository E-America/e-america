/**
 * Site Configuration
 *
 * Update the BASE_URL when deploying to production or changing domains.
 * This URL is used for absolute URLs in meta tags (OG images, etc.)
 */
export const SITE_CONFIG = {
  BASE_URL: "https://e-america.org",
} as const
